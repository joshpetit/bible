name: bible
description: A simple application to interact with multiple bible APIs from a single place
version: 0.6.0
homepage: https://github.com/joshpetit/bible/

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: ^0.12.2
  reference_parser: ^1.2.2

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
